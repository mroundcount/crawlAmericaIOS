# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Crawl America' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Crawl America
  pod 'Firebase/Core'
  pod 'FirebaseAuth'
  pod 'FirebaseDatabase'
  pod 'FirebaseFirestore'
  pod 'ProgressHUD'

  target 'Crawl AmericaTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Crawl AmericaUITests' do
    # Pods for testing
  end

end
